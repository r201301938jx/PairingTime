<% provide(:title, "投稿一覧") %>

<div class="row">
  <%= render 'sort_form' %>
  <%= render 'search_form' %>
</div>

<div class="row">
  <h2 class="heading text-center">投稿一覧（全<%= @pairs.count %>件）</h2>

  <%= render 'index', pairs: @pairs %>
  <div class="text-center col-sm-12"><%= paginate @pairs %></div>

</div>
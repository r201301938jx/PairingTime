<div class="row">
  <%= render 'search_form' %>
</div>

<div class="row">
  <h2 class="heading text-center">「<%= @content %>」と一致した会員一覧（全<%= @records.count %>件）</h2>

  <%= render 'index', customers: @records %>

</div>
<div class="row">
  <%= render 'search_form' %>
</div>

<div class="row">
  <h2 class="heading text-center">会員一覧（全<%= @customers.count %>件）</h2>

  <%= render 'index', customers: @customers %>

</div>


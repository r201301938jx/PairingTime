<% provide(:title, "ログイン") %>

<div class="row　col-sm-offset-3 col-sm-6 col-sm-offset-3 col-12">
  <h2 class="heading text-center">ログイン</h2>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

  <div id="error_explanation">
    <%= flash[:alert] %>
    <%= flash[:error] %>
  </div>

  <div class="form-group row">
    <%= f.label :メールアドレス, class: "col-sm-3 col-4" %>
    <div class="col-sm-9 col-8">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
    </div>
  </div>

  <div class="form-group row">
    <%= f.label :パスワード, class: "col-sm-3 col-4" %>
    <div class="col-sm-9 col-8">
      <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
    </div>
  </div>

  <div class="col-sm-12 col-12 text-right">
    <%= link_to "=> パスワードを忘れた方はこちら", new_password_path(resource_name), class: "btn-link" %>
  </div>

  <div class="actions">
    <%= f.submit "ログイン", class: "btn btn-brown btn-block" %>
  </div>
  <% end %>

  <div class="col-sm-12 col-12 text-center">
    <p>登録がお済みでない方は<%= link_to "こちら", new_customer_registration_path %>から新規登録を行ってください。</p>
  </div>

</div>

